# REDLINE Known Issues

| # | Area | Issue | Severity | Notes |
|---|------|-------|----------|-------|
| K-1 | Validation | No human has played M1, M2 or the M3 slice yet. All checks so far are headless or scripted: 130 automated tests, a route bot, a movement probe, a performance probe and screenshot tours. How the game feels and how real input latency behaves are unverified. | High (process) | This is the M1/M2/M3 gate. See the reports' checklists. |
| K-2 | Performance | FPS was only observed under software rendering (Mesa llvmpipe in a virtual display), at about 45–55 FPS. That number doesn't represent real GPUs. CPU cost is measured headless by wall time per frame: about 0.6–0.9 ms idle, and under Combat Lab fight load 2.2 ms on average, 4.9 ms at p99 and 11 ms at worst (`devtools/PerfProbe.tscn`), against a 16.7 ms budget. **Correction:** the earlier "1.5 ms / 3.7 ms physics" figures came from `Performance.TIME_PHYSICS_PROCESS`, which is unreliable in fixed-fps headless runs. | Medium | Verify on real hardware, including GPU cost. |
| K-3 | Rendering | Physics runs at 60 Hz without interpolation by default, which may look juddery on 120/144 Hz displays. The F7 (interpolation) and F8 (120 Hz) toggles exist, but nobody has checked them on a high-refresh screen. Whether `Camera2D` is interpolated in 4.3 without jitter is also unverified. | Medium | Experiments in the report and D-011. |
| K-4 | Rendering | Camera and player positions aren't snapped to pixels (`snap_2d_transforms_to_pixel` is off). Sprites may shimmer by a subpixel once real art arrives. | Low | Revisit with sprites in M3. |
| K-5 | Movement | Slides are fully committed: you can't steer or brake, and slopes don't change slide speed. | Low | Design call after the playtest. |
| K-6 | Movement | Corner correction only applies to upward motion. There's no horizontal corner correction for dashes clipping ledge lips. | Low | Add it if playtesters snag on corners. |
| K-7 | Settings | ~~Nothing saves settings.~~ Fixed in M3: the settings menu saves. Input rebinding still doesn't exist. | Low | Rebinding is in TODO. |
| K-8 | Input | Debug hotkeys F2–F12 (and the mouse-driven tuning panel) are keyboard-only. | Info | Dev tools only (D-006). |
| K-9 | UI | World-space station labels use the default font at 8 px and look soft. | Info | Placeholder. |
| K-10 | Tests | The class cache has to exist before tests run: `godot --headless --import` must run once on a fresh clone. | Info | Documented in the README. |
| K-11 | Audio | The SFX are synthesized placeholder blips: good enough to judge timing, not tone. The mix levels haven't been tuned on speakers. | Info | Real sounds replace them through `override_stream`. |
| K-12 | Tools | Tuning panel sliders use each property's export range, or 0–3× its current value. To go beyond that range, edit the `.tres` file. Collision sizes aren't on the panel because they need `apply_config`. | Info | By design. |
| K-13 | Tools | Saving from the panel while the default preset is active rewrites `data/movement/default_movement.tres`. Commit only the tuning changes you mean to keep. | Info | Revert (or `git checkout`) undoes it. |
| K-14 | Combat | The numbers (damage, hitstop, telegraphs, core drain) haven't been felt by a person. | High (process) | M2 gate. |
| K-15 | Combat | Enemies pass through Rook, and Rook can stand inside them (D-024). | Low | Design call after the playtest. |
| K-16 | Combat | No parry or executions yet. Heal injectors and an elite (Enforcer) arrived in M3. | Info | Later milestones. |
| K-17 | Tools | The F3 tuning panel covers movement only. Combat data is edited in `.tres` files; F5 reloads movement only, so restart to pick up weapon/enemy edits. | Low | Follow-up in TODO. |
| K-18 | Engine | In Godot 4.3, calling a method on a freed *typed* reference crashes the engine instead of raising an error. Code must check `is_instance_valid` before touching any object that might have been freed (projectile shooters, credited attackers). | Medium | See CLAUDE.md conventions. |
| K-19 | Audio | Quitting while a sound is playing reports one leaked `AudioStreamPlaybackWAV`. It's harmless, even though voices are stopped in `_exit_tree`. | Info | Engine-side. |
| K-20 | Performance | Spawning enemies and first-use effects cause one 11 ms frame. There's no pooling yet. | Low | Pool if it shows up on real hardware. |
| K-21 | Lab | Combat Lab station labels and the debug overlay overlap on some views. F1 hides the overlay. | Info | Placeholder layout. |
| K-22 | Art/Audio | **Everything is placeholder** (D-026): rectangles, props, a procedural skyline, synthesized SFX and music. Bible M3 asks for production quality; that has to come from artists and composers, following `ART_BIBLE.md`. | High (process) | Human decision in TODO. |
| K-23 | Content | The slice may be shorter than the bible's 15–25 min: pure bot traversal takes ≈ 2 min. Real play time is unmeasured. Since M7 this applies to the debug-only Relay-start slice and the report's "Lowlight time" line; New Game is the longer Act I campaign (K-45). | Medium | Measure in the playtest (end card), then decide on D-028 options. |
| K-24 | Movement | A slide-jump beats a run-jump by only about 8–15 px, and only when it's timed at the lip. A late slide does worse than a run-jump. The Neon Roofs gap is a teaching gap, not a gate (D-036). | Medium | Tuning call after the playtest. |
| K-25 | Audio | The music stems are rendered at startup on a worker thread (1.3 s of CPU on the 4-core build container). Music fades in once they're ready. Headless runs skip it, so no automated test hears the final mix. | Low | Real stems replace it (Art Bible §10). |
| K-26 | World | There's no map, fast travel or objective markers (D-033). The only shortcut is the Bell Tower lift back to the Relay. | Medium | M5 scope. Watch the "did you get lost?" answers. |
| K-27 | World | Dying again before you recover a Scrap cache loses the older cache (D-031). This is intended, but it can feel harsh. The Settings toggle turns Scrap loss off. | Info | Design confirmation wanted. |
| K-28 | Tools | `RouteBot` proves that rooms are *traversable* with enemies disabled. It doesn't prove they're fun or fair with enemies on, and it doesn't fight the boss (boss behavior has its own tests). | Info | By design. Humans judge fights. |
| K-29 | UI | Menus and hints use the default font at native resolution. There's no pixel font or final UI art yet. Long hint strings can wrap near the screen edges on narrow aspect ratios (only 16:9 has been checked). | Low | UI art pass. |
| K-30 | Playtest | Frame times are wall-clock per rendered frame, including vsync waits. On a 60 Hz display the average reads ~16.7 ms even with headroom. Use the spike counts and the 33 ms+ buckets as the stutter signal. | Info | By design; documented in the report. |
| K-31 | Playtest | Idle-span detection can't tell reading or thinking from being lost. | Info | Cross-check with moments and facilitator notes. |
| K-32 | Playtest | The first frames after a room load include one-off loading spikes (e.g. one 55 ms frame entering the Warden Tower in a bot run). | Low | Repeated spikes are the problem; singles on entry are expected. |
| K-33 | Playtest | A crash keeps the session up to the last autosave (every 20 s, and on every death). | Info | `autosave_interval` in `PlaytestConfig`. |
| K-34 | Playtest | No exported build is committed: `export_presets.cfg` stays gitignored, and export templates must be installed in the editor. | Info | Steps in `PLAYTEST_KIT.md` §2. |
| K-35 | Map | The map redraws every explored cell and geometry piece each frame. Fine for the 7-room slice; since M7 the map holds 18 world rooms in two districts plus the Relay, so caching is due before batch 2. | Low | TODO (M5 follow-ups). |
| K-36 | Map | The cursor pans freely; it doesn't snap to rooms or icons. At the widest zoom, small icons overlap. | Low | Human check. |
| K-37 | Map | The labs have no map (the map is world rooms only). | Info | By design. |
| K-38 | Transit | Travel starts only from an Anchor menu, not from the map (D-047). | Low | Design call. |
| K-39 | Tools | ~~The slice's room `.tscn` files were produced by a Python generator that isn't committed.~~ Fixed in M6 (`tools/roomgen`, D-056). The scenes are the source of truth and editable in Godot, but bulk layout edits are manual until M6 tooling. | Medium | M6 content pipeline. |
| K-40 | Tools | Template baking, rebuilds on property changes and configuration warnings are editor behaviour. Tests only cover `build()` and the logic; nobody has tried them in the Godot editor yet. | Medium | Human check (TODO). |
| K-41 | Tools | `tools/roomgen` needs Python 3 (dev only). Editor edits make a room drift from its script (`--check` reports it). | Info | D-056. |
| K-42 | Tools | The dev console exists in every *debug* export. Playtest builds must be exported as release. | Low | D-059. |
| K-43 | Art | Rook's sprite swap-in maps movement states plus attack ids. Heal, interact and death poses need animation names agreed when the real art arrives. | Info | Art Bible §6 list. |
| K-44 | Movement | K-24 now also touches the main path: Rainline Chase's G3 is a 112 px slide-jump under the Sweeper (D-074). A late slide falls short. | Medium | The LowRoad catch below G3 costs no pip and keeps the chase going. Watch chase catches per checkpoint in the playtest report. |
| K-45 | Pacing | The Undercity's length is estimated, not measured: Relay arrival at 19.5 / 28.25 / 37 min (low / mid / high), Dash at 43 / 59.5 / 76. Fast players reach the Relay 10 min before §42's 30–60 band (D-083). | Medium | The report's Undercity timeline measures it. If the median Relay arrival is under ~25 min, deepen Medical Ruin and First Pursuit. |
| K-46 | Art / accessibility | The Undercity's sea-green tubes (0.62, 0.8, 0.72) sit near healing green (`#7dff9a`). Colourblind players may confuse ambience with a heal cue. | Low | Placeholder palette; check with the final art and a colourblind filter. |
| K-47 | Art | The Undercity theme draws light vertical "seepage" rain (`rain = true`, 26 drops). It may read as weather underground. | Info | Fallback: `rain = false` in `data/districts/undercity.tres`. |
| K-48 | Level design | Dodge-jump + late air dodge vs Dash gates. On the flat it reaches ~229 px against the dash-jump's ~242, more with a take-off late in the coyote window. **Smuggler Route** (282 px, 64 px drop) is swept clean at the lip and 8 px past it, but a take-off 16–20 px past the lip plus an air dodge on the exact frame still lands ("Too wide to jump (mostly)"). **Flooded Alley** (290 px, 64 px drop) is swept clean. **Escape Tunnel** (`cs_uc_tunnel_dash`, 230 px at one height) is swept only for delays 0..27 from the step edges and probably leaks the same way. | Medium | D-094. Widen the tunnel sweep like `_smuggler_sweep`, and lower its ledge if it leaks. A mastery trick, not a soft-lock: nothing on the main path needs Dash. |
| K-49 | Economy | One full re-clear pays 332 Scrap against a cap of 334 (15% of stock). The next respawning enemy placed anywhere fails `test_economy`. | Medium | D-049. Add sinks (the next district's stock) before enemies; run `--filter=economy` after any enemy or drop change. |
| K-50 | Core | In Challenge mode, lingering about 11 s or more in Escape Tunnel's FZ3 (full drain, no floor) burns. | Info | Accepted as the mode's contract (D-085). |
| K-51 | Tests | `test_fz3_pace` has almost no margin in Normal: the Core reaches 0 at about 19.6–20.5 s of its 20 s run and a burnout pip would land at about 21 s. | Low | Intended as a guard: any change to FZ3 drain, kill rewards or blade `reactor_gain` fails it. Re-measure and re-tune together. |
| K-52 | Playtest | Power Block's S4b is passed low under a closing panel, but `shutter_passed` reports the open clock (~0.2 s), so the report lists it as a close call (D-099). | Low | Read that row as expected, or make `PowerShutter` report low passes against the slot clock. |
| K-53 | Art | Decor banners draw no text: Medical Ruin's "PULSE EXTRACTION" and Collector Bay's "CIVIC RECOVERY → BELL TOWER LIFT" exist only as generator comments. The chest-clamp chairs are silhouettes. Orr's radio in Escape Tunnel floats 40 px above the top floor. | Info | Final art (D-026). |
| K-54 | Enemies | The Collector eye's lock builds only while Rook is nearly still; jumping in place still builds it, any real movement drains it (D-069). Perfect dodges against its bolt are rare in open play, because `Projectile`'s per-frame ray misses sideways dodges against a near-vertical bolt. | Low | Playtest: does the eye still feel threatening? Watch the "Collector eye locks" table. |
| K-55 | Enemies | A Needle with the default aggro range (200) placed on a ledge above a walkway wakes early, follows Rook to the lip and lunges off (D-097). | Low | Use `needle_ledge.tres` (aggro 160) or keep ledges out of range. |
| K-56 | Boss | The blade `BossBot` beats the Collector Drone in about 26 s, near the 25 s lower bound of its test. | Low | Collector HP (400) is the first knob if playtesters find it short. |
| K-57 | Tools | RouteBot pitfalls: (1) a `runjump` right after a `jump` landing can fire at once, because a small backward drift passes its keep-speed check; put a short `run` step first. (2) `shoot … diag` holds `move_x`, so a multi-shot diagonal volley walks Rook forward; use single diagonal shots per step. | Info | `CONTENT_PIPELINE.md` lists them. |
| K-58 | Playtest | The "Slice (Relay start)" title entry (D-068) exists only in debug builds, but playtest builds should be exported as release (D-059). | Low | Human decision (D-068). |
| K-59 | UI | A hint line can outlive its room. In the slice capture tour, the Smuggler den shot (taken a few seconds after Security Station) still shows Security Station's calibration line. Since D-103 hints queue (2 s minimum hold), so a queued hint may also appear after a door. | Low | Seen only with the tour's instant teleports; check whether taking a door right after a hint does the same in play. |
| K-60 | Tests | Headless runs end with "ObjectDB instances leaked at exit" and "2 resources still in use at exit". The exit code is 0 and no test fails. | Info | Already printed before the M7 rooms landed; harmless. |
| K-61 | Level design | Escape Tunnel's pistol lesson hits the Watcher diagonally only from a narrow band of floor x (about 214–226 in an audit sweep), and RouteBot shoots from one x (222). A straight-up shot from x ~404–416 also hits, although the generator comment says the bracket blocks it. | Medium | Widen the band (larger hurtbox, lower perch or a marked firing spot) and sweep x in a test (≥ 48 px must hit). Not changed in the audit fix pass. |
| K-62 | Level design | Power Block's floors L0–L3 have no nearby respawn: its Anchor is on L4 and Neon Roofs has none, so a death on the trench spikes or the L3 fight returns to Apartment Stack `stack_mid`, two rooms back. | Medium | Add an Anchor or `EntryCheckpoint` near the L0 entrance. Watch deaths by room in the playtest report. |
| K-63 | Code | Some tuning is still in code, not data: `ChaseDirector` WARN_SHAKE, HINT_SECONDS and the proximity-tick interval; `ScannerBeam` HITSTOP and the per-node offline_open/offline_warn exports; `GridClamp`'s 1-pip hit; `TrackerConfig.RUN_SPEED`, which copies the 150 px/s run speed instead of reading the movement config. | Low | Move them into PursuerData, ScannerData, ClampTiming and TrackerConfig when those systems are next touched. |

## M8 (narrative integration)

| # | Area | Issue | Severity | Notes |
|---|------|-------|----------|-------|
| K-M8-1 | Pacing | First-time Act I gains ~73 s of locked scenes at Normal subtitle speed (opening 17.0, Collector 8.2, arrival 11.7, Krail 12.6, close 23.2 s) plus ~20–30 s for the first-rest memory (D-135). | Medium | Budgets are validator-enforced; every scene is skippable; repeat intros cost no control. The §44 report measures first-view skip rate and `cinematic_s`. If first-view skips pass 50%, cut the opening to its two lines. |
| K-M8-2 | Input | First-time mashing: the tap/hold split (a tap never skips, a hold never advances), the 0.25 s grace and carried-press rejection mitigate it, but a pad player who holds A for 0.8 s still skips a first view. | Low | Watch `seq_end.step` and the first-view skip rate in §44 data. |
| K-M8-3 | Tests | INSTANT (the headless default) hides sequence timing bugs from route tests. | Low | Covered by AUTO skip-parity tests and `CaptureTour --tour=story`. |
| K-M8-4 | Tests | Route tests now set new flags (`seen_seq_uc_opening`, `arc_*` on `met_*`), so a test that compares whole flag dictionaries can break. | Info | Compare only the flags a test owns; never weaken assertions. |
| K-M8-5 | Performance | `count:secrets` calls `SliceStats.totals()`, which instantiates every world room once per process. | Low | Warmed at boot via `DataDir.list_scenes`; watch for a hitch on the first arc evaluation after `met_nix`. |
| K-M8-6 | Tools | Regenerating a room renumbers `Neon#`/`Decor#` where decor moved into switches (Wake, Bell Tower, Warden Tower, the Relay). | Info | Nothing may reference numbered names (the validator forbids it for sequences). Append new nodes at the end of a room script. |
| K-M8-7 | Tools | `lowlight.py`, `uc_wake.py` and `uc_collector_bay.py` were edited serially by two tasks; any later edit must regenerate on top and rerun every `--check`. | Info | All generators match their scenes at 0.8.0. |
| K-M8-8 | UI | A memory replay over the paused journal must not unpause a tree it did not pause; DialogueBox choice mode keeps the tree paused. | Info | Covered by tests; keep it in mind when touching `MemoryScenePlayer`. |
| K-M8-9 | Tests | Automation (RouteBot, CaptureTour) always picks choice 0 on Orr's on-air beat, which sets `orr_air_named`; tour states carry it. | Info | Tests reset flags per test. |
| K-M8-10 | Art | The Relay gains ~12 world-state props; its art rule is "almost no cyan". Orr's repeater pips are the sanctioned radio cyan. | Low | Review the `st_relay_*` story tour shots (human). |
| K-M8-11 | World | Mara moves from her workbench to the alley door after `act1_complete`; returning players may look for her at the bench. | Low | Her map pin moves with her; revert is data-only. |
| K-M8-12 | Story | Canon commitments (D-109, D-113, D-114, D-134, D-137) need human confirmation before Act II writing starts. | Medium (process) | TODO. |
| K-M8-13 | Story | Placeholder ending text ships in data (minable); it reveals no mechanism and every card says PLACEHOLDER ENDING. `data/endings` is exempt from the knowledge lint. | Low | D-137 review. |
| K-M8-14 | UI | DialogueBox rendering must stay pixel-identical at default settings (label y+12, text y+25, 62 px floor). | Info | `test_dialogue_box_geometry` and the story tour's `st_dialogue_size2` pin it; tests and CaptureTour pin settings to defaults. |
| K-M8-15 | Save | Legacy saves past `slice_end_seen` never see the Act I close (they get `act1_complete` on load); saves that arrived but never met Orr see the arrival once more; arc beats for stages reached before M8 become pending. | Info | Accepted (D-116). |
| K-M8-16 | Story | Redline cannot be reached before M9 (D-130); the Release thresholds are provisional (D-129). | Info | Act V design. |
| K-M8-17 | Validation | A task whose data reads a flag produced by content not yet merged fails `ValidateContent`. | Info | `act1_complete` is a code flag; future flags are declared. |
| K-M8-18 | Accessibility | Subtitle speed Slower doubles line time (~73 s → ~130 s of locked lines). | Info | Opt-in; budgets are checked at ×1.0; every scene stays skippable. |
| K-M8-19 | UI | No pause menu while a DialogueBox conversation or Orr's choice is open (D-136); the tree is paused, so nothing progresses. Memory vignettes have their own pause panel (Resume / Skip memory / Subtitle size). | Low | M9 fix (TODO). Watch §44 notes. |
| K-M8-20 | Code | Shared files had serial owners (CombatHud, MusicDirector, CaptureTour, DialogueBox). | Info | Rebase on the merged edit and keep its tests green. |
| K-M8-21 | Tests | New `class_name` scripts need `godot --headless --import` in every checkout and after each one is added; otherwise the runner fails with 'not declared' and hangs. | Info | See K-10. |
| K-M8-22 | Export | Exported builds list `.tres.remap` / `.tscn.remap`. Every M8 runtime scan goes through `DataDir.list` / `list_scenes`; a direct `.tres` filter would silently drop memories, speaker labels and the Act I card. | Medium | Smoke-test an exported build before the playtest (TODO). |
| K-M8-23 | Engine | A fire-and-forget coroutine on a RefCounted object stops at its first `await` in 4.3. | Info | `EndingDirector` retains itself and dev replays are static coroutines; any new M8 coroutine object must do the same. |
| K-M8-24 | Cinematics | An abort runs no `finish()`: world state a step's `run()` leaves outside the restore contract (an NPC mid-move, a pose) stays partial. Actor position and modulate are reverted. | Low | Aborts happen only on a room leave, Save & Quit or test teardown (rooms reload). A future sequence that aborts in place must not rely on `finish()`. |
| K-M8-25 | Cinematics | `cinematic_lock` gates `PlayerCombat` and `ScannerBeam`, but other direct `take_damage` paths still hurt a locked Rook: the `ChaseDirector` catch, `Room._pit_fall` and `PlayerCombat._check_hazards`. | Low | No Act I sequence plays over a chase, pit or hazard. Add a `cinematic_lock` guard before placing one there. |
| K-M8-26 | Cinematics | `Cinematics.play()` aborts a running bark before starting a locking play; if a `sequence_finished` listener starts another play inside that abort, the new play is replaced without being aborted. | Info | No content triggers it. |
| K-M8-27 | Economy | `chart_lowlight`'s reward flag `map_lens` is also Nix's shop counter: completing the quest writes `true` over a bought int 1 (the shop then reads 2). | Low | Pre-existing; `FlagSandbox.apply_act1_max_state` normalises it for dev states. Data fix in TODO. |
| K-M8-28 | Cinematics | `SliceEndTrigger` has no `CinematicMode.theatre` guard (`SequenceTrigger` has one). `replay_act1_close` works around it inside its sandbox. | Low | Anything else playing in the Relay with Krail defeated and `slice_end_seen` unset could start the close by walking in. |
| K-M8-29 | Tools | In the story tour's room and Relay shots, HUD hints queued by applying a preset ('QUEST COMPLETE — Dead Air', 'CIRCUIT ACQUIRED — Longline') can still show after 180 settle frames. | Info | Shots stay readable. A HUD hint-queue flush for tours is in TODO. |
| K-M8-30 | Audio | `MusicDirector` leaves its MEMORY state only on `memory_playback_finished`; an abort (`CinematicMode.abort_all`, teardown) emits nothing. An Anchor's one-shot playback listener also stays connected after an abort. | Info | Unreachable in play (no menu opens over a vignette); tests reset it. If an in-play abort path is added, clear the state there (e.g. on `room_loaded`). |
| K-M8-31 | World | Nix's M7 NPC post in the Relay (x 330, box 318..342) overlaps the pillar at 340. | Info | Pre-M8 placement, untested; placeholder art. |
| K-M8-32 | Tests | Headless exits still print engine noise: 'Parse JSON failed' (the corrupt-save test, recovered from backup), a `P12GodotShape2D` RID leak, 'resources still in use', TextServer / `FontVariation` leaks (the italic narration face) and 'p_elem->_root != this'. Exit code 0, no test fails. | Info | Extends K-60. |
| K-M8-33 | Tests | `test_capture_tour_defaults_instant` compares the modification time of the shared `user://settings.cfg`; another process writing it during the test could make it flaky. | Info | Not seen so far. |
| K-M8-34 | UI | The main Settings page is 259 of 270 px tall (11 px left). | Info | A new main-page row needs another sub-page or a scroll. |
| K-M8-35 | Story | The Act I card always shows the fallback line, so only 2 conditional standing lines fit: on a completed Act I the Orr and Nix lines usually take both, and the ledger, Iko and memories lines rarely show (D-131). | Info | Reorder `data/story/act1.tres` or raise `max_standing` if the card has room. |
| K-M8-36 | Tools | `TestRunner` only takes `--filter` after a `--` separator; without it the whole suite runs. | Info | The commands in CLAUDE.md use the separator. |
| K-M8-37 | Tools | The story tour's `st_act1_card_menu_no_dead_air` is byte-identical to `st_act1_card_menu`: `CaptureTour._story_act_card` clears `dead_air_complete` with `Game.set_flag`, and `QuestTracker` re-derives it from its stage flags, so the "still deaf" standing line is never captured. | Info | Write `Game.state.flags` directly (as `test_act1_card_header_and_standing` does) and rebuild the card. |
