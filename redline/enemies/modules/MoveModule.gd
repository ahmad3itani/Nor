class_name MoveModule
extends Resource
## Locomotion while engaging: returns the desired velocity (and sets facing).


func engage_velocity(_b: ModularBehavior, _delta: float) -> Vector2:
	return Vector2.ZERO
