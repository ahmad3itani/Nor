[gd_resource type="Resource" script_class="PresenceTable" load_steps=2 format=3]

[ext_resource type="Script" path="res://platform/PresenceTable.gd" id="1_pt"]

[resource]
script = ExtResource("1_pt")
title = "In the menus"
room = "{district} — {room}"
boss = "Fighting {boss}"
memory = "Remembering"
cinematic = "Watching"
act_done = "Act I complete — {room}"
lab = "In the {room}"
challenge = "Challenge — {name}"
